[package]
name = "tm20"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
readme.workspace = true
description = "Typed ESC/POS commands and USB, serial, and TCP transports for Epson TM-T20 printers"
keywords = ["escpos", "epson", "thermal", "printer"]
categories = ["hardware-support", "encoding"]

# Protocol, encode, and Raster build everywhere. Transports need a host OS.
[target.'cfg(not(target_arch = "wasm32"))'.dependencies]
nusb = "0.2"
serialport = { version = "4", default-features = false }

[lints]
workspace = true
